import { createAction, props } from '@ngrx/store';

export const loadNotificationScreens = createAction(
  '[NotificationScreen] Load NotificationScreens'
);

export const removeNotificationScreens = createAction(
  '[NotificationScreen] remove NotificationScreens'
);
